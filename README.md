# Gulp & Sass+Inky & BrowserSync

How to install:
```
mkdir letters
cd letters

git clone https://github.com/Rekryt/inky-letters .
rm -rf .git #optional

npm install
```

How to install gulp:
```
npm install gulp-cli -g
```